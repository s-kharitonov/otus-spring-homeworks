spring:
  data:
    mongodb:
      port: 0
      database: library-test
  shell:
    interactive:
      enabled: false
logging:
  level:
    root: error
mongock:
  runner-type: initializingbean
  change-logs-scan-package: ru.otus.changelogs